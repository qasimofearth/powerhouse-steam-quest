import { BrownFatInteraction } from '../interactives/interface';

const interaction: BrownFatInteraction = {
  type: 'brown-fat',
  ariaLabel: 'Slide between making energy and making heat',
};

export default interaction;
