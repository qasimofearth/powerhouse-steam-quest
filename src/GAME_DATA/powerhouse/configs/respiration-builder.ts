import { RespirationBuilderInteraction } from '../interactives/interface';

const interaction: RespirationBuilderInteraction = {
  type: 'respiration-builder',
  ariaLabel: 'Feed fuel and oxygen into a mitochondrion and watch ATP come out',
};

export default interaction;
