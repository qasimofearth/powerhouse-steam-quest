import { AtpSynthaseInteraction } from '../interactives/interface';

const interaction: AtpSynthaseInteraction = {
  type: 'atp-synthase',
  ariaLabel: 'Pump protons and spin the ATP synthase motor',
};

export default interaction;
