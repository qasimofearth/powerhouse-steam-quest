import { SprintIntensityInteraction } from '../interactives/interface';

const interaction: SprintIntensityInteraction = {
  type: 'sprint-intensity',
  ariaLabel: 'Raise the effort and watch oxygen demand outrun supply',
};

export default interaction;
