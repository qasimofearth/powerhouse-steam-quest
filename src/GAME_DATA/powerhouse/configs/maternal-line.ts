import { MaternalLineInteraction } from '../interactives/interface';

const interaction: MaternalLineInteraction = {
  type: 'maternal-line',
  ariaLabel: 'Trace your maternal line back to Mitochondrial Eve',
};

export default interaction;
