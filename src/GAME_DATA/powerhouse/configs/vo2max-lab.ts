import { Vo2maxLabInteraction } from '../interactives/interface';

const interaction: Vo2maxLabInteraction = {
  type: 'vo2max-lab',
  ariaLabel: 'Run a treadmill VO2max test',
};

export default interaction;
