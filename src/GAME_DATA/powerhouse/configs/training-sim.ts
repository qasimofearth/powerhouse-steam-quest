import { TrainingSimInteraction } from '../interactives/interface';

const interaction: TrainingSimInteraction = {
  type: 'training-sim',
  ariaLabel: 'Design a training week and grow mitochondria',
};

export default interaction;
