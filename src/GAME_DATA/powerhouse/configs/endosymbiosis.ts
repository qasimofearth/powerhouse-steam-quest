import { EndosymbiosisInteraction } from '../interactives/interface';

const interaction: EndosymbiosisInteraction = {
  type: 'endosymbiosis',
  ariaLabel: 'Collect evidence that mitochondria were once bacteria',
};

export default interaction;
